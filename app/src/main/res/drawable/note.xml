<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- pressed -->
    <!-- focused -->
    <!-- hovered -->
    <!-- default -->
    <item android:state_pressed="true" android:drawable="@drawable/img_note" />
    <item android:state_focused="true" android:drawable="@drawable/img_note" />
    <item android:state_hovered="true" android:drawable="@drawable/img_note" />
    <item android:drawable="@drawable/img_note" />
</selector>